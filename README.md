# Лабораторные работы по Параллельному Программированию

В данном репозитории содержатся решения лабораторных задач по курсу параллельного программирования. Проект разделен на две части, соответствующие темам **MPI** и **Shared Memory / GPU**.

Подробные инструкции по запуску и описания алгоритмов находятся в `README.md` внутри каждой папки.

## Структура репозитория

### [📂 lab1](./lab1) — Технология MPI
Реализация параллельных алгоритмов с использованием интерфейса передачи сообщений (**MPI**).

**Решенные задачи:**
1.  **Вычисление числа $\pi$:** Метод Монте-Карло (попадание точек в окружность).
2.  **Умножение матрицы на вектор:** Реализация различных разбиений (строковое, столбцовое, блочное).
3.  **Матричное умножение:** Блочное умножение по алгоритму Кэннона.
4.  **Задача Дирихле:** Стационарное распределение тепла (метод Гаусса-Зейделя с волновой схемой).

---

### [📂 lab2](./lab2) — OpenMP, CUDA и Pthreads
Реализация параллельных вычислений на системах с общей памятью и на графических процессорах.

**Решенные задачи:**
1.  **Множество Мандельброта (OpenMP):** Параллельная генерация фрактала.
2.  **Задача N тел (OpenMP + CUDA):** Моделирование движения частиц под действием гравитации. Сравнение производительности CPU и GPU реализаций.
3.  **Блокировки чтения-записи (Pthreads):** Собственная реализация примитивов синхронизации `rwlock` (rdlock, wrlock) и тестирование на односвязном списке.

## Требования к окружению
*   **Компиляторы:** `gcc` / `g++`, `mpicc`, `nvcc` (NVIDIA CUDA Toolkit).
*   **Библиотеки:** OpenMPI (или аналоги), OpenMP, Pthreads.